# Minimal dependency set required to run the app + test suite offline.
# The full requirements.txt adds real integration SDKs (kubernetes,
# prometheus, google-generativeai, ...) which are lazily imported and
# therefore NOT needed for the offline/mock test path.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pytest>=8.0
pytest-asyncio>=0.23
